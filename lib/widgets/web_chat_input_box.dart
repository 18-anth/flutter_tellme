import 'package:flutter/material.dart';

class web_chat_input_box extends StatelessWidget {
  const web_chat_input_box({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}