import 'package:flutter/material.dart';

class web_chat_List extends StatelessWidget {
  const web_chat_List({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}